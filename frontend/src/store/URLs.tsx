import { atom } from 'jotai';

export const runningURL = atom([]);
export const completedURL = atom([]);
export const brokenLinkCount = atom([]);
export const internalLinks = atom([]);
export const externalLinks = atom([]);
export const analysisList = atom([]);
export const links = atom([]);
export const userId = atom(null);